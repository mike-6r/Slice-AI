# Frontend adapter mapping

Future mapping: asset/market/portfolio/collector/wallet/notification/community repositories map to documented `/api/v1` resources, carry the canonical request/response DTOs, invalidate matching query keys, and consume their named future events. Current adapters remain mocks; no frontend change is made.
