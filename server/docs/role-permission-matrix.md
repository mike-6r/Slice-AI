# Role/permission matrix

USER has own profile/session actions. SUPPORT additionally has `user.read.support`. ADMIN has admin access, role management, account-status change, other-session revocation and identity-audit read. Other roles have no Phase 2 identity privilege. Suspended/closed actors are denied; self-escalation is denied. Future asset/vault/finance permissions are reserved only.
