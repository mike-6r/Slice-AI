# Setup

Setup persists canonical panel message IDs and managed resource logical keys in PostgreSQL. Repair is non-destructive: it reports ambiguous matches and never chooses or removes duplicate user content automatically.
