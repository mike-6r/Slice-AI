# Notification roles

Only eight logical notification roles are self-assignable. The role select compares desired values against the strict allowlist, resolves the guild-scoped managed role IDs, applies Discord changes, and writes `DiscordNotificationPreference`. Staff and verification roles are never accepted as selector values.
