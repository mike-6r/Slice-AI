import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js';
export const embedCommand = new SlashCommandBuilder().setName('embed').setDescription('Build and publish a Slice embed').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addSubcommand((s) => s.setName('create').setDescription('Create an interactive embed draft').addStringOption((o) => o.setName('name').setDescription('Draft name').setRequired(true).setMaxLength(100))).addSubcommand((s) => s.setName('edit').setDescription('Edit a saved draft').addStringOption((o) => o.setName('draft').setDescription('Draft ID').setRequired(true))).addSubcommand((s) => s.setName('list').setDescription('List saved drafts')).addSubcommand((s) => s.setName('clone').setDescription('Clone a saved draft').addStringOption((o) => o.setName('draft').setDescription('Draft ID').setRequired(true)).addStringOption((o) => o.setName('name').setDescription('New draft name').setRequired(true).setMaxLength(100))).addSubcommand((s) => s.setName('import').setDescription('Import validated embed JSON').addStringOption((o) => o.setName('name').setDescription('Draft name').setRequired(true)).addStringOption((o) => o.setName('json').setDescription('Safe embed JSON').setRequired(true).setMaxLength(4000)));
