import { describe, expect, it } from 'vitest'; import { mkdtemp } from 'node:fs/promises'; import { tmpdir } from 'node:os'; import { join } from 'node:path'; import sharp from 'sharp'; import { renderArtwork } from '../../src/artwork.js';
describe('artwork renderer', () => { it('renders Discord-safe PNG banners', async () => { const dir = await mkdtemp(join(tmpdir(), 'slice-art-')); const path = await renderArtwork(dir, 'welcome', 'Welcome to Slice'); const meta = await sharp(path).metadata(); expect(meta.format).toBe('png'); expect([meta.width, meta.height]).toEqual([1200, 400]); }); });
